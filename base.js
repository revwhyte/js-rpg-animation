// animacao da borda da div, transformando de quadrado pra circulo
// function animate() {
//     var TIMES = 50.0, SPEED = 0.0, STEP = 0.5;
//     var elem = document.getElementById("dado");
//     var id = setInterval(frame, SPEED);
//     function frame() {
//         log(SPEED, TIMES);
//         if(SPEED == TIMES) {
//             clearInterval(id);
//         }
//         else {
//             SPEED += STEP;
//             log(`speed ${SPEED}`);
//             elem.style.borderRadius = `${SPEED}px`;
//         }
//     }
// }